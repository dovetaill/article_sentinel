# Showcase: `showcase/multisurface`

`showcase/multisurface` 是 PureMux 的完整业务示例分支，保留了比 `main` 更重的模块集合、路由面和示例叙事。

之所以独立出去，是为了让 `main` 保持为一个干净、容易上手的 starter，而不是继续混合 starter 与 showcase 两种目标。

如果你想查看那套更完整的参考应用，请执行：

```bash
git switch showcase/multisurface
```
